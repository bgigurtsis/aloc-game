{
  "name": "aloc-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A browser game about the Loss-of-Control Threat Matrix for agentic AI",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "node --experimental-strip-types --no-warnings --test 'tests/**/*.test.mjs'"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.1.0"
  }
}
